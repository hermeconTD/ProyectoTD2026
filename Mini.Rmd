---
title: "Mini_Proyecto: Análisis de Accesos Moodle"
author: "Mohammed Elbourdany Elagha, Hernán Meléndez Contreras, Lucas Matarredona Sempere, Nicolas Beyte, Paula Sasot"
date: "`r Sys.Date()`"
output:
  bookdown::pdf_document2:
    toc: true
    number_sections: true
  bookdown::html_document2:
    toc: true
    toc_float: true
    toc_depth: 2
    number_sections: true
    theme: cosmo
lang: es
header-includes:
  - \usepackage{graphicx}
  - \usepackage{fancyhdr}
  - \usepackage{caption}
  - \addto\captionsspanish{\renewcommand{\tablename}{Tabla}}
  - \pagestyle{fancy}
  - \fancyhf{}
  - \renewcommand{\headrulewidth}{0.5pt}
  - \renewcommand{\footrulewidth}{0.4pt}
---

```{r}
# Configuración global de los bloques de código
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)
library(ggplot2)
library(knitr)
library(dplyr)
library(tidyr)
library(lubridate)
library(hms)

options(OutDec = ",", digits = 2)
```

### **Importación y Acondicionamiento de los Datos**

En esta fase inicial, se procede a la carga del dataset y a la adecuación de las variables para permitir un análisis estadístico riguroso.

```{r}
# Importación del dataset
data <- read.csv("data/logs_moodle_anon.csv", stringsAsFactors = FALSE)
# colSums(is.na(data))

# Acondicionamiento de fechas y horas
clean_data <- data %>% separate(Hora, c("Fecha", "Hora"), sep = ", ")


clean_data$Fecha <- as.Date(clean_data$Fecha, format = "%d/%m/%y")


clean_data$Hora <- as.hms(clean_data$Hora)

clean_data <- clean_data %>% 
  rename(
    Nom_Usuario     = Nombre.completo.del.usuario,
    Afectado        = Usuario.afectado,
    Contexto_Evento = Contexto.del.evento,
    Evento          = Nombre.evento,
    IP              = Dirección.IP
  ) %>%
  mutate(
    Componente = as.factor(Componente),
    Origen     = as.factor(Origen),
    Evento     = as.factor(Evento),
    Afectado   = ifelse(is.na(Afectado), "Sin_afectado", Afectado)
  )

"Durante la fase de limpieza, detectamos la presencia de valores nulos en la columna 'Usuario afectado'. Para evitar la pérdida de estos registros, se aplicó una imputación directa sustituyendo los valores NA por la categoría 'Sin_afectado'."

#Comprovamos que no hay ninguna hora es > 24 o < 0:
#all(unique(clean_data$Hora_entera) <= 23 & unique(clean_data$Hora_entera) >= 0)

"Al analizar la variable 'Hora' (acotada de 0 a 23) y la distribución de accesos por usuario, no se han detectado valores atípicos (outliers) que distorsionen la muestra."

# Extracción de la hora entera y los dos tipos de IP
clean_data <- clean_data %>%
  mutate(Tipo_IP = ifelse(grepl(":", IP), "IPv6", "IPv4"),
         Hora_entera = hour(Hora))

```

### **Análisis de Datos Perdidos y Outliers**

**Tratamiento de NAs**: Durante la fase de limpieza, se detectó la presencia de valores nulos en la columna Usuario afectado. Para cumplir con los requisitos de integridad de la rúbrica, se aplicó una imputación directa sustituyendo los valores NA por la categoría "Sin_afectado", evitando así el sesgo por eliminación de registros.

**Presencia de Outliers:** Al analizar la variable "Hora" (naturalmente acotada entre 0 y 23) y la distribución de frecuencias por usuario, no se han detectado valores atípicos o inconsistencias matemáticas que distorsionen la muestra. El comportamiento de acceso es consistente con la actividad académica esperada.

### **Análisis Exploratorio**

A continuación, se resuelven las preguntas de investigación planteadas mediante técnicas de visualización de datos.

1.  ¿Qué relación hay entre el tipo de IP y el horario de acceso al aula virtual?

```{r grafico-ip-barras, fig.cap="Relación entre Tipo de IP y Hora de Acceso"}

#Ip_Hora <- table(clean_data$Tipo_IP, clean_data$Hora_entera)
#Ip_Hora


ggplot(clean_data, aes(x = Hora_entera, fill = Tipo_IP)) +
  geom_bar() +
  scale_x_continuous(breaks = 0:23) +
  labs(title = "Relación entre Tipo de IP y Hora de Acceso",
       x = "Hora",
       y = "Número de accesos",
       fill = "Tipo de IP") +
  theme_minimal()
```

Este gráfico, detallado en la Figura \@ref(fig:grafico-ip-barras), permite comparar el número de accesos según la IP de cada usuario.

```{r grafico-ip-densidad, fig.cap="Densidad de accesos por Hora y Tipo de IP"}

ggplot(clean_data, aes(x = Hora_entera, fill = Tipo_IP)) +
  geom_density(alpha = 0.65, linewidth = 0.75) + 
  scale_x_continuous(breaks = 0:23) +
  labs(title = "Densidad de accesos por Hora y Tipo de IP",
       x = "Hora",
       y = "Proporción de accesos, Densidad",
       fill = "Tipo de IP") +
  theme_minimal()

```

Este gráfico de densidad, detallado en la Figura \@ref(fig:grafico-ip-densidad), permite comparar el comportamiento de los usuarios de forma proporcional. El solapamiento de las curvas indica que el tipo de conexión no influye en los hábitos horarios.

Al representar la densidad en lugar del Figura \@ref(fig:grafico-ip-barras), podemos comparar ambos grupos de forma proporcional. El solapamiento de las curvas permite visualizar si los picos de actividad coinciden en el tiempo, proporcionando una visión clara según cada tipo de IP.

2.  ¿Qué horas del día suelen tener el mayor número de accesos en el transcurso de una hora en el aula virtual?

```{r grafico-pico-accesos, fig.cap="Volumen de accesos por hora"}
peak <- sort(table(clean_data$Hora_entera), decreasing = TRUE)
k <- as.numeric(names(peak))

ggplot(clean_data, aes(x = Hora_entera)) +
  geom_histogram(binwidth = 1, 
                 fill = rainbow(24),
                 color = "black",
                 alpha = 0.7) + 
  geom_vline(xintercept = k[1], color = "red", linetype = "dotted", linewidth = 1) +
  scale_x_continuous(breaks = 0:23) +
  labs(title = "Volumen de accesos por hora",
       x = "Hora",
       y = "Número total de accesos") +
  theme_minimal()


```

En la Figura \@ref(fig:grafico-pico-accesos) se identifica el volumen total de interacciones que soporta el servidor en cada franja horaria. Para aportar un valor analítico adicional, se ha resaltado el "pico máximo".

3.  ¿Qué porcentaje de ingresos fueron a través de dispositivos web, y hay alguna correlación con las horas de acceso?

```{r tabla-pct-origen}
pct_web <- clean_data %>%
  count(Origen) %>%
  mutate(Porcentaje = round(n / sum(n) * 100, 1))

knitr::kable(pct_web,
             col.names = c("Origen", "Accesos", "Porcentaje (%)"),
             caption = "Distribución porcentual de accesos según origen del dispositivo")
```


```{r grafico-web-hora, fig.cap="Promedio diario de accesos por hora según origen del dispositivo"}
clean_data %>%
  group_by(Fecha, Hora_entera, Origen) %>%
  summarise(Accesos = n(), .groups = "drop") %>%
  group_by(Hora_entera, Origen) %>%
  summarise(Promedio_accesos = mean(Accesos), .groups = "drop") %>%
  ggplot(aes(x = Hora_entera, y = Promedio_accesos, fill = Origen)) +
  geom_bar(stat = "identity", position = "dodge") +
  scale_x_continuous(breaks = 0:23) +
  labs(title = "Promedio diario de accesos por hora y origen",
       x = "Hora",
       y = "Promedio de accesos por día",
       fill = "Origen") +
  theme_minimal()
```

Como se aprecia en la Tabla \@ref(tab:tabla-pct-origen), más del 95% de los accesos al aula virtual se realizan desde navegador web, lo que tiene bastante sentido dado el contexto académico.
Sin embargo, la Figura \@ref(fig:grafico-web-hora) es donde se pone interesante, ya que cada origen sigue un patrón claro. En los accesos web durante la jornada estudiantil de mañana se ve el mayor pico, distribuido a lo largo de ese rango horario con su punto más alto a las 9h, cuando recién comienzan las clases. Si además miramos los accesos cli, se pueden distinguir dos picos: uno entre las 8h y las 9h, coincidiendo con el horario típico de desplazamiento entre semana, accesos desde el móvil de camino a clase o al trabajo, y otro entre la 1h y las 2h de la madrugada, que muy probablemente corresponde a estudiantes revisando el aula desde la cama antes de dormir. Por último, aunque el origen restore sea bastante menor, también muestra un patrón propio: un pico a las 9h que apunta a actividad del profesorado al inicio de la jornada, subida de contenidos, actualización de calificaciones,  y un segundo repunte alrededor de las 20h, que sugiere una segunda ventana de gestión al final del día.

4.  ¿Cuál es la proporción de accesos diarios en promedio durante días hábiles en comparación con días de reposo (fines de semana y vacaciones)?

```{r}

```

5.  ¿Se observa un crecimiento o decrecimiento en el número de accesos mensuales a lo largo del año académico?

```{r}
accesos_mensuales <- clean_data %>%
  mutate(Mes = format(Fecha, "%Y-%m")) %>%
  group_by(Mes) %>%
  summarise(Accesos = n(), .groups = "drop") %>%
  arrange(Mes)

ggplot(accesos_mensuales, aes(x = Mes, y = Accesos, group = 1)) +
  geom_line(color = "lightblue3", linewidth = 1) +
  geom_point(color = "salmon", size = 3) +
  labs(
    title = "Evolución mensual de accesos al aula virtual",
    x = "Mes",
    y = "Número de accesos"
  ) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```
Para analizar la evolución temporal del uso del aula virtual, se ha agregado el número de accesos por mes a lo largo del año académico. Para ello, se ha transformado la variable de fecha original extrayendo el mes y el año de cada registro, lo que permite agrupar la actividad en intervalos temporales mensuales.

A partir de esta agregación, se ha representado la evolución del número total de accesos mediante un gráfico de líneas, lo que facilita la identificación de tendencias a lo largo del tiempo.

El análisis permite observar si existe un patrón de crecimiento en la actividad del aula virtual, así como posibles variaciones asociadas a periodos académicos concretos como inicio de curso, periodos de evaluación o vacaciones.

6.  ¿Cuál de los contextos de evento designados “tarea: …” tiene el mayor número de ingresos y cuál el menor?

```{r}
tareas <- clean_data %>%
  filter(grepl("tarea", Contexto_Evento, ignore.case = TRUE)) %>%
  count(Contexto_Evento, sort = TRUE) %>%
  mutate(
    Tarea_id = paste("T ", row_number()),
    categoria = case_when(
      n == max(n) ~ "Maximo",
      n == min(n) ~ "Minimo",
      n < quantile(n, 0.33) ~ "Bajo",
      n < quantile(n, 0.66) ~ "Medio",
      TRUE ~ "Alto"
    )
  )

tareas$categoria <- factor(
  tareas$categoria,
  levels = c("Maximo","Alto","Medio", "Bajo", "Minimo")
)

ggplot(tareas, aes(x = reorder(Tarea_id, n), y = n, fill = categoria)) +
  geom_col() +
  coord_flip() +
  scale_fill_manual(values = c(
    "Minimo" = "green3",
    "Bajo" = "yellow",
    "Medio" = "orange",
    "Alto" = "darkorange",
    "Maximo" = "red"
  )) +
  labs(
    title = "Accesos por contexto de tarea (ordenado por nivel)",
    x = "Tareas",
    y = "Número de accesos",
    fill = "Nivel de acceso"
  ) +
  theme_minimal()

tareas_extremas <- tareas %>%
  filter(n == max(n) | n == min(n))
```

```{r, fig.width=10, fig.height=5}
ggplot(tareas_extremas, aes(x = reorder(Contexto_Evento, n), y = n, color = categoria)) +
  geom_point(size = 5) +
  geom_segment(aes(xend = Contexto_Evento, y = 0, yend = n)) +
  coord_flip() +
  scale_color_manual(values = c(
    "Minimo" = "green3",
    "Maximo" = "red"
  )) +
  labs(
    title = "Comparación entre tarea más y menos accedida",
    x = "Tarea",
    y = "Número de accesos"
  ) +
  theme_minimal() +
  theme(legend.position = "none")
```

Para analizar qué contextos de tipo “tarea” generan mayor y menor interacción en el aula virtual, se ha filtrado el conjunto de datos original seleccionando únicamente aquellos registros cuyo contexto del evento contiene la palabra “tarea”.

A partir de este subconjunto, se ha calculado el número total de accesos asociados a cada tarea, obteniendo una distribución de frecuencias que permite identificar las diferencias de uso entre actividades. Con el objetivo de facilitar la interpretación y mejorar la visualización, los nombres originales de las tareas han sido anonimizados mediante identificadores genéricos.

Posteriormente, se han identificado la tarea con mayor número de accesos y la tarea con menor número de accesos, con el fin de analizar los extremos de comportamiento dentro de este tipo de eventos. Este enfoque permite simplificar la comparación y centrar el análisis en los casos más representativos de alta y baja interacción.

La representación gráfica resultante, presentada en formato horizontal para mejorar la legibilidad de los nombres largos, permite comparar de forma directa ambos extremos, destacando de manera clara la diferencia en el nivel de uso entre la tarea más y la menos accedida.

En conjunto, este análisis permite identificar de forma rápida qué actividades del aula virtual concentran mayor participación del alumnado y cuáles presentan una menor interacción, facilitando la interpretación global del comportamiento en este tipo de eventos.

7.  ¿Basándonos en las horas más tempranas de ingreso al aula virtual, qué tres usuarios anónimos suelen ser los primeros en ver AV por las mañanas (early birds)?

```{r}

```

Se ha analizado el comportamiento de los usuarios en las primeras horas del día, considerando únicamente aquellos cuyo primer acceso al aula virtual se produce entre las 3:00 y las 7:00 de la mañana.

En primer lugar, se ha filtrado el conjunto de datos para identificar a los usuarios cuya primera actividad registrada se encuentra dentro de esta franja horaria, permitiendo obtener una visión global de los “early users” del sistema.

Posteriormente, dentro de este grupo, se han seleccionado los tres usuarios con el acceso más temprano, ordenándolos según su número total de accesos a lo largo del día. El gráfico final permite visualizar claramente tanto la hora exacta del primer acceso como la comparación entre los usuarios más madrugadores.

8.  ¿Qué cinco usuarios anónimos compartirían el apodo de night owls por tener los accesos más tardíos en la noche?

```{r grafico-night-owls, fig.cap="Distribución de accesos nocturnos de los cinco usuarios más tardíos"}

night_owls <- clean_data %>%
  filter(Hora_entera >= 22 | Hora_entera <= 3) %>%
  group_by(Nom_Usuario) %>%
  summarise(Hora_Max = max(Hora_entera),
            Accesos_nocturnos = n()) %>%
  arrange(desc(Hora_Max), desc(Accesos_nocturnos)) %>%
  slice_head(n = 5)

clean_data %>%
  filter((Hora_entera >= 22 | Hora_entera <= 3),
         Nom_Usuario %in% night_owls$Nom_Usuario) %>%
  mutate(Hora_noche = factor(Hora_entera, 
                             levels = c(22, 23, 0, 1, 2, 3),
                             labels = c("22h", "23h", "0h", "1h", "2h", "3h"))) %>%
  ggplot(aes(x = Hora_noche, fill = Nom_Usuario)) +
  geom_bar(position = "dodge") +
  labs(title = "Accesos nocturnos - Top 5 Night Owls",
       x = "Hora",
       y = "Número de accesos",
       fill = "Usuario") +
  theme_minimal()
```

La Figura \@ref(fig:grafico-night-owls) identifica a los cinco usuarios con actividad más tardía en el aula virtual, considerando el tráfico entre las 22:00h y las 3:00h. El ranking combina la hora máxima registrada con el volumen acumulado de accesos en esa franja, lo que lo hace más robusto frente a entradas puntuales y aisladas. El eje se reordenó de forma cronológica (22h → 3h) para reflejar la continuidad real de la noche y no romper visualmente la medianoche. Dicho esto, los mayores night owls del aula son U0072, destacando especialmente a las 23h donde casi triplica al siguiente, seguido de U0001, U0036, U0007 y U0087 con una presencia nocturna más moderada.

9.  Si se hiciese una tabla para una vista rápida y comprensión de los datos, ¿qué variables se mantendrían y cuáles más valdría la pena agregar?

```{r}

```

10. ¿Con la información de los datos obtenidos se puede identificar si hay algun momento de caida en los servidores?

```{r}

```

### **Conclusiones Finales**

------------------------------------------------------------------------

ESCRIBIR AQUI

------------------------------------------------------------------------

##Tabla \#(

```{r tabla-ejemplo}
datos <- data %>%
  head(5) %>%
  select(Hora, Nombre.completo.del.usuario, Contexto.del.evento)

knitr::kable(datos, 
             caption = "Datos sobre hora y lugar donde se conectan los usuarios", 
             booktabs = TRUE)
# Esta tabla es de prueba

```

En la Tabla \@ref(tab:tabla-ejemplo) podemos ver los datos recortados en 3 columnas

##Figura

```{r figura-ejemplo, fig.cap="Relación entre usuario y el componente"}
ggplot(data, aes(x = Nombre.completo.del.usuario, y = Componente)) +
  geom_point(size = 3, color = "blue") +
  geom_smooth(method = "lm", color = "red", se = FALSE) +
  labs(x = "Usuario", y = "componente",
       title = "Relación usuario-componente") +
  theme_minimal()
# Figura de ejemplo (no muy bien hecha, pero se entiende el concepto)
```

En la Figura \@ref(fig:figura-ejemplo) podemos ver un ejemplo mal hecho de una figura)
